add_undo(bg)
add_undo(x)
add_undo(y)
add_undo(tlayer)
add_undo(tile_get_left(tile))
add_undo(tile_get_top(tile))
add_undo(tile_get_width(tile))
add_undo(tile_get_height(tile))
add_undo(tilesx)
add_undo(tilesy)
add_undo(image_blend)
add_undo(image_alpha)
